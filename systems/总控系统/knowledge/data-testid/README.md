# 总控系统 data-testid 映射索引

> **状态**：待创建  
> **说明**：总控系统尚无 data-testid 映射文件。待总控系统测试迭代启动后，由前端开发在 DOM 元素上添加 data-testid 属性（使用 `/add-data-testid` 命令），再按模块拆分创建映射文件。

---

## 命名规范

参照项目统一规范：`{模块编号}_{场景编号}_{序号}`（如 `01_S1-01_01`）

## 模块分配表

> 待总控系统功能模块确定后填写。

| 模块编号 | 模块名称 | 负责文件 | 状态 |
|---------|---------|---------|------|
| 01 | （待定） | 01_xxx.md | 未创建 |

---

*最后更新：2026-04-10*
