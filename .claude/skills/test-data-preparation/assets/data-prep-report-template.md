# 测试数据准备报告模板

> 本文件是 test-data-preparation skill 的输出模板，由 SKILL.md 在 Phase 5 输出报告时加载。
> **替换此文件即可改变报告结构，无需修改 SKILL.md 中的指令逻辑。**

---

## 数据准备报告

```markdown
# 测试数据准备报告

**生成时间**：{YYYY-MM-DD HH:mm:ss}
**技术文档**：{文档名称/路径}
**用例文件**：{用例文件名称/路径}

---

## 1. 准备概况

| 指标 | 值 |
|------|-----|
| API 接口总数 | {N} |
| 数据创建请求数 | {N} |
| 成功 | {N} |
| 失败 | {N} |
| 供给用例数 | {N} |

---

## 2. 调用计划

### 接口清单

| # | 接口名称 | Method | Endpoint | 必填参数 | 依赖接口 |
|---|---------|--------|----------|---------|---------|
| 1 | 用户登录 | POST | /auth/login | username, password | 无 |
| 2 | 创建XXX | POST | /xxx | name, type | #1(获取token) |

### 依赖关系图

```
登录(#1) → 创建A(#2) → 创建B(#3)
                      ↘ 创建C(#4)
```

---

## 3. 执行记录

| # | 接口 | 请求参数(关键) | 响应状态 | 提取数据 | 耗时 |
|---|------|-------------|---------|---------|------|
| 1 | POST /auth/login | admin/Test1234 | 200 OK | token=eyJ... | 320ms |
| 2 | POST /xxx | name="{数据名称}_{时间戳}" | 200 OK | id="xxx_001" | 450ms |

---

## 4. 创建数据清单

| # | 数据描述 | 创建方式 | 关键标识 | 验证特征 | 供给用例 | 状态 |
|---|---------|---------|---------|---------|---------|------|
| 1 | {数据描述} | POST /xxx | id=xxx_001 | {验证特征描述} | PRJ-XXX-003 | 成功 |

---

## 5. 变量映射表

> 以下变量可在测试用例中通过 `{引用:变量名}` 使用

| 变量名 | 值 | 来源 |
|--------|-----|------|
| {var_name} | {value} | POST /xxx 响应 |
| auth_token | eyJ... | POST /auth/login 响应 |

---

## 6. 残留数据检测记录

| # | 数据描述 | 检测方式 | 检测结果 | 处理方式 |
|---|---------|---------|---------|---------|
| 1 | {数据描述} | GET /xxx?name={关键词} | 已存在(id=xxx_old) / 不存在 | 已删除残留，重新创建 / 直接创建 |

---

## 7. 失败项（如有）

| # | 数据描述 | 失败原因 | 影响用例 | 建议处理 |
|---|---------|---------|---------|---------|
| - | - | - | - | - |

---

## 8. 数据清理指引

> 按依赖关系逆序清理：先删子数据，后删父数据。

| # | 数据 | 清理方式 | 清理接口/路径 | 清理顺序 | 清理时机 |
|---|------|---------|-------------|---------|---------|
| 1 | {子数据描述} | API | DELETE /xxx/child/{id} | 1（先清理） | 用例执行完毕后 |
| 2 | {父数据描述} | API | DELETE /xxx/{id} | 2（后清理） | 全部用例执行完毕后 |

---

## 9. 数据就绪验证清单

> 供 AI 执行用例前快速验证数据仍然就绪。

| # | 数据描述 | 验证方式 | 验证命令/路径 | 预期结果 | 供给用例 |
|---|---------|---------|-------------|---------|---------|
| 1 | {数据描述} | API | GET /xxx/{id} | status=200, {字段}匹配 | PRJ-XXX-003 |
| 2 | {数据描述} | UI | 「{页面}」搜索"{关键词}" | 列表可见, {字段}={值} | PRJ-XXX-004 |

---

## 10. 数据清理执行记录（用例执行后填写）

| # | 数据描述 | 清理方式 | 清理结果 | 备注 |
|---|---------|---------|---------|------|
| 1 | {数据描述} (id={xxx}) | DELETE /xxx/{id} | 成功(200) / 失败({原因}) | - |
```
