# {迭代名称} — 设计摘要

> **一句话描述**：{本次迭代做了什么，解决什么业务问题，影响哪些核心模块}
>
> 本文档是 AI 详设文档的浓缩，用于人工审阅。阅读时间目标：5 分钟。
> 完整实现细节见：[迭代变更总纲](./{版本号}-{迭代名称}-迭代变更总纲.md)

---

## 一、核心决策

> 列出本次迭代中做出的关键设计决策及理由，重点关注有选择空间的决策点。

| 决策点 | 选定方案 | 备选方案 | 选择理由 |
| --- | --- | --- | --- |
| 事务边界 | {单事务 / 分布式事务 / 最终一致性 / 无} | {备选} | {1句理由} |
| 并发策略 | {乐观锁 / 分布式锁 / 无} | {备选} | {1句理由} |
| 幂等方案 | {唯一索引 / Redis防重 / 幂等Token / 无} | {备选} | {1句理由} |
| {其他决策点} | {方案} | {备选} | {理由} |

---

## 二、变更范围速览

| 维度 | 内容 |
| --- | --- |
| **涉及域** | {域A}（{N} 个接口）、{域B}（{M} 个接口） |
| **新增接口** | {接口名1}、{接口名2} |
| **修改接口** | {接口名3}（变更点：{简述}） |
| **数据库变更** | {新增表 `{table}`} / {`{table}` 加字段 `{column}`} / 无 |
| **外部依赖变更** | {新增 Manager: `{ManagerName}` 调用 `{外部服务}`} / 无 |
| **代码文件** | 新增 {N} 个，修改 {M} 个 |

---

## 三、风险提示

> 仅列出需要额外关注的风险，无风险时填「无」。

- 🔴 **{高风险描述}**：{如"订单和库存写入同一事务，高并发时锁竞争风险"}
- 🟡 **{中风险描述}**：{如"历史数据迁移约 500 万条，建议分批执行"}
- 🟡 **{中风险描述}**：{如"新增字段未加索引，查询量大时注意性能"}

---

## 四、偏差审阅清单

> 请逐项确认，发现偏差请回复「❌ {清单项编号}：{具体描述}」，无问题回复「✅ 全部确认」。

### 4.1 业务规则

- [ ] **{BR-01}** `{接口名}`：{核心业务规则描述，如"当订单状态为已支付时才允许发货，否则返回错误码 40201"}
- [ ] **{BR-02}** `{接口名}`：{业务规则，如"库存扣减失败时整体回滚，不允许超卖"}
- [ ] **{BR-03}** {状态机/流程规则描述}

### 4.2 接口契约

- [ ] **{IC-01}** `{METHOD} {/api/v1/path}`：入参 `{字段名}` 类型为 `{类型}`，是否与前端约定一致
- [ ] **{IC-02}** `{METHOD} {/api/v1/path}`：错误码 `{code}` 对应场景「{场景描述}」，是否与调用方约定一致
- [ ] **{IC-03}** 分页接口返回字段 `{字段}` 是否覆盖了前端所需的展示数据

### 4.3 数据模型

- [ ] **{DM-01}** 表 `{table_name}` 新增字段 `{column}` 类型为 `{类型}`，长度/精度是否符合业务取值范围
- [ ] **{DM-02}** `{DtoClassName}` 的字段是否覆盖了「{使用场景}」的全部数据需求
- [ ] **{DM-03}** 索引 `{idx_name}` 是否覆盖了主要查询路径

### 4.4 事务 / 并发

- [ ] **{TC-01}** `{接口名}` 事务范围：步骤 {1~N} 在事务内，步骤 {N+1} 在事务外，边界是否正确
- [ ] **{TC-02}** `{接口名}` 并发策略「{策略}」，在 {QPS 量级/并发场景} 下是否足够
- [ ] **{TC-03}** `{接口名}` 幂等策略「{策略}」，是否能覆盖「{重复提交场景}」

---

*如发现偏差，AI 将定位到详设文档对应章节修复并告知变更内容。*
