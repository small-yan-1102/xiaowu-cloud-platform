# 缺陷回归验证报告

> **生成时间**: {YYYY-MM-DD HH:mm}
> **迭代 / 版本**: {version_or_sprint}
> **关联缺陷报告**: `{bug_report_path}`
> **执行环境**: {test_env_url}

---

## 一、回归概览

| 指标 | 数量 |
|------|------|
| 待回归缺陷总数 | {total} |
| ✅ 回归通过（建议关闭）| {passed} |
| ❌ 回归失败（建议重新打开）| {failed} |
| ⏭️ 跳过（条件未满足）| {skipped} |

**整体结论**：{overall_conclusion}
> 示例：「所有 Fatal/Critical 缺陷已通过回归，建议重新触发 release-gate」
> 示例：「仍有 1 条 Critical 回归失败，暂不建议发布」

---

## 二、逐条回归结果

### ✅ 通过 → 建议在云效关闭

| 缺陷编号 | 缺陷标题 | 严重等级 | 关联用例 | 云效工作项 ID |
|---------|---------|---------|---------|-------------|
| {BUG-MODULE-001} | {title} | {severity} | {case_id} | {yunxiao_id} |

> 操作指引：将以上工作项状态更新为「**已关闭**」

---

### ❌ 失败 → 建议在云效重新打开

| 缺陷编号 | 缺陷标题 | 严重等级 | 关联用例 | 失败步骤 | 期望 | 实际 |
|---------|---------|---------|---------|---------|------|------|
| {BUG-MODULE-002} | {title} | {severity} | {case_id} | 步骤 {N} | {expected} | {actual} |

> 操作指引：将以上工作项状态更新为「**待处理**」，并在评论区注明：
> 「回归失败（{date}）: {actual_result_summary}」

---

### ⏭️ 跳过（条件未满足）

| 缺陷编号 | 缺陷标题 | 跳过原因 |
|---------|---------|---------|
| {BUG-MODULE-003} | {title} | {reason}（如：测试数据未就绪 / 环境异常 / 无法映射用例）|

> 以上跳过项暂不变更云效状态，待条件满足后安排复测。

---

## 三、残留风险

> 如果所有严重等级均为「通过」或「跳过（无 Fatal/Critical）」，此章节可省略。

| 缺陷编号 | 严重等级 | 状态 | 风险说明 |
|---------|---------|------|---------|
| {BUG-MODULE-002} | Critical | 回归失败 | 主流程{功能}仍存在缺陷，影响{用户操作路径} |

---

## 四、后续建议

```
[ ] 在云效完成以上缺陷状态更新操作
[ ] 所有 Fatal/Critical 已关闭 → 触发 release-gate 重新评估发布条件
[ ] 存在跳过项（Fatal/Critical）→ 待条件满足后安排复测，再决策发布
[ ] 有失败项需开发再次修复 → 修复后重新触发 defect-retest
```

---

*由 AI 缺陷回归验证工具自动生成 | defect-retest skill v1.0*
