# 前端 PRD 审阅报告（模板）

> **产出人**：前端开发工程师
> **接收人**：产品经理
> **用途**：前端开发从界面实现可行性、交互逻辑、组件设计、前端性能等维度审阅 PRD 和产品原型，向产品经理反馈需要澄清、补充或修正的问题，确保 PRD 达到可进入前端技术设计的标准。
> **使用说明**：`{花括号}` 内为需要填写的内容，`<!-- 说明 -->` 为填写指引（最终文档中删除）。

---

**审阅信息**

| 项目 | 内容 |
|------|------|
| PRD 文档名称 | {PRD 文档名称及版本} |
| 原型/设计稿 | {原型文件名称或链接，无则填"无"} |
| 审阅人 | {前端开发工程师姓名} |
| 审阅日期 | {YYYY-MM-DD} |
| 期望回复日期 | {YYYY-MM-DD} |

<!-- 为什么需要这部分：建立文档的追溯性，明确审阅对象和责任人。PRD 经常迭代，不标明版本会导致"我审的是旧版"的混乱。期望回复日期推动产品经理在合理时间内响应。 -->

---

## 一、审阅结论摘要

<!-- 为什么需要这部分：让产品经理一眼了解整体情况，快速判断 PRD 是否需要重大修改还是微调。前端特有的"复杂度评估"直接影响排期决策。 -->

| 维度 | 结果 |
|------|------|
| 审阅的功能模块数 | {N} |
| 界面实现问题（UI） | {N} 个 |
| 交互逻辑问题（IX） | {N} 个 |
| 组件与状态问题（CM） | {N} 个 |
| 前端性能风险（PF） | {N} 个 |
| 前端业务逻辑问题（BL） | {N} 个 |
| 需求歧义（AM） | {N} 个 |
| 完整性缺失（CP） | {N} 个 |
| 非功能需求缺失（NF） | {N} 个 |
| 整体评估 | {可进入技术设计 / 修改后可进入技术设计 / 需重大补充} |

**前端整体复杂度评估**：

| 维度 | 评估 |
|------|------|
| 前端工作量级别 | {小 / 中 / 大} |
| 技术风险等级 | {低 / 中 / 高} |
| 涉及页面数 | {N} |
| 预估组件数 | {N} |
| 复杂交互功能点 | {N} |
| 需要特殊技术方案的功能点 | {N} |

---

## 二、界面实现可行性问题（UI 系列）

<!-- 为什么需要这部分：前端的核心职责是将产品设计还原为可交互的界面。如果原型中的视觉效果或布局方式在技术上难以实现或成本过高，需要提前沟通。同时，不同的 UI 方案可能需要引入不同的第三方库，影响技术选型和包体积。 -->

> 界面实现问题 = PRD/原型中的页面布局、视觉效果、数据展示方式存在技术实现难度或定义不清，导致前端无法确定实现方案。

| # | 所属模块 | 问题描述 | PRD/原型位置 | 影响范围 | 建议处理方式 | 产品回复 |
|---|---------|---------|-------------|---------|------------|---------|
| UI-1 | {模块名} | {具体问题描述} | {PRD 章节或原型页面} | {影响哪些页面/组件} | {建议如何补充/修改} | |
| UI-2 | | | | | | |

<!-- 常见的界面实现问题类型：
1. 布局方案不明：页面中某区域的布局方式不清晰，无法确定使用何种 CSS 方案
2. 视觉规格缺失：动画效果、渐变色、阴影等缺少具体参数
3. 数据展示方式不明：不清楚用表格、卡片还是列表展示
4. 图表类型未定义：需要数据可视化但图表类型和交互行为未明确
5. 素材依赖不明：不清楚图标库、字体、插图等素材从哪里来
6. 富文本需求边界不清：支持哪些格式、是否支持图片/视频嵌入
7. 响应式策略未定义：不同屏幕下的布局变化未说明
-->

---

## 三、交互逻辑问题（IX 系列）

<!-- 为什么需要这部分：交互逻辑直接决定前端代码的控制流。PRD 中"看起来简单"的一个交互，到前端可能需要处理多种状态、多个异步流程和各种边界条件。如果交互逻辑不完整，前端要么自行"脑补"（后期必然产生争议），要么每个细节都找产品确认（效率极低）。 -->

> 交互逻辑问题 = PRD 中用户操作的触发方式、过程反馈、结果展示定义不完整，前端无法确定交互实现方案。

| # | 所属模块 | 问题描述 | PRD/原型位置 | 对交互实现的影响 | 建议处理方式 | 产品回复 |
|---|---------|---------|-------------|----------------|------------|---------|
| IX-1 | {模块名} | {具体问题描述} | {PRD 章节或原型页面} | {无法确定什么} | {建议如何补充/修改} | |
| IX-2 | | | | | | |

<!-- 常见的交互逻辑问题类型：
1. 操作反馈缺失：用户点击按钮后看到什么反馈？成功/失败分别展示什么？
2. 确认流程不明：危险操作是否需要二次确认？确认弹窗内容和按钮是什么？
3. 页面跳转不明：操作完成后跳转到哪里？新窗口还是当前窗口？
4. 加载体验未定义：数据请求中用户看到什么？是否可以取消？
5. 表单交互不完整：校验时机、校验失败展示方式、表单重置规则不明确
6. 多步骤流程规则缺失：步骤间的依赖关系、是否支持跳步、数据如何暂存
7. 拖拽行为不明确：拖拽范围、放置规则、排序更新时机
8. 实时更新策略不明：什么时候刷新数据？是否有新数据到达的视觉提示？
-->

---

## 四、组件与状态设计问题（CM 系列）

<!-- 为什么需要这部分：组件和状态是前端架构的核心。PRD 中对数据展示结构的定义直接影响组件拆分方式，而跨页面数据共享的需求决定了状态管理方案的复杂度。如果这些信息不清晰，前端技术设计阶段会充满不确定性。 -->

> 组件与状态问题 = PRD 中的数据展示结构、跨页面数据关系、表单联动规则定义不清，导致前端无法确定组件体系和状态管理方案。

| # | 所属模块 | 问题描述 | PRD/原型位置 | 对组件/状态设计的影响 | 建议处理方式 | 产品回复 |
|---|---------|---------|-------------|-------------------|------------|---------|
| CM-1 | {模块名} | {具体问题描述} | {PRD 章节或原型页面} | {无法确定什么} | {建议如何补充/修改} | |
| CM-2 | | | | | | |

<!-- 常见的组件与状态设计问题类型：
1. 列表字段不完整：表格/卡片需要展示哪些字段未列全
2. 跨页面状态关系不明：A 页面的操作是否影响 B 页面的展示？如何同步？
3. 表单联动规则不完整：字段 A 的值变化如何影响字段 B/C/D 的行为？
4. 数据缓存策略不明：页面切换时筛选条件/滚动位置是否保留？
5. 权限驱动 UI 规则不全：不同角色下页面元素的具体差异不明确
6. 复用模式不明：多个页面中相似功能是否期望一致的交互和展示？
-->

---

## 五、前端性能风险（PF 系列）

<!-- 为什么需要这部分：前端性能直接影响用户体验。产品经理说"展示列表"，但数据量是 100 条还是 10 万条，前端方案完全不同。大数据量可能需要虚拟滚动、分页、懒加载等特殊处理。如果不在需求阶段确认，上线后才发现页面卡顿，修复成本远高于提前规划。 -->

> 前端性能风险 = PRD 中的功能描述存在潜在的前端渲染/加载性能隐患，需要产品确认数据量级或交互复杂度，以便前端选择合适的技术方案。

| # | 所属模块 | 风险描述 | PRD/原型位置 | 需确认的信息 | 不同场景下的方案差异 | 产品回复 |
|---|---------|---------|-------------|------------|-------------------|---------|
| PF-1 | {模块名} | {风险描述} | {PRD 章节或原型页面} | {需要产品确认什么} | {场景不同方案如何变化} | |
| PF-2 | | | | | | |

<!-- 常见的前端性能风险类型：
1. 大数据量列表：未说明数据量级，可能需要虚拟滚动、服务端分页等不同方案
2. 首屏资源过重：首屏需加载大量数据和资源，需确认是否可以延迟加载
3. 大文件上传/预览：文件大小影响是否需要分片上传、流式预览
4. 复杂图表渲染：数据量大的图表可能导致页面卡顿，需确认数据采样策略
5. 实时数据更新：频繁更新 DOM 的性能影响，需确认更新频率
6. 前端计算密集：大量数据的排序、筛选、统计在前端执行的可行性
7. 图片资源过多：图片密集页面的加载策略（懒加载、CDN、压缩）
-->

---

## 六、前端业务逻辑问题（BL 系列）

<!-- 为什么需要这部分：虽然核心业务逻辑在后端执行，但前端也承载大量业务逻辑——表单校验、条件展示、数据格式化、联动计算等。这些逻辑需要前端在代码中硬编码，如果 PRD 定义不完整，上线后必然出现"这里的行为不对"的 Bug。 -->

> 前端业务逻辑问题 = PRD 中需要前端实现的业务规则（校验、展示、计算、联动）存在缺失或歧义，前端无法确定实现逻辑。

| # | 所属模块 | 问题描述 | PRD 位置 | 对前端实现的影响 | 建议处理方式 | 产品回复 |
|---|---------|---------|---------|----------------|------------|---------|
| BL-1 | {模块名} | {具体问题描述} | {PRD 章节编号} | {导致什么实现困难} | {建议如何补充/修改} | |
| BL-2 | | | | | | |

<!-- 常见的前端业务逻辑问题类型：
1. 表单校验规则不完整：字段的必填/格式/长度/范围/自定义校验规则未定义
2. 条件展示逻辑不明：页面元素在什么条件下显示/隐藏/禁用未说明
3. 计算公式缺失：前端需要计算的合计、百分比、倒计时等缺少公式或精度要求
4. 数据格式化规则不明：日期、金额、数字的展示格式未定义（如金额保留几位小数）
5. 联动规则不完整：字段之间的级联/联动关系未列全
6. 状态展示规则缺失：不同状态用什么颜色/图标/标签、操作按钮如何变化未定义
7. 权限控制规则不全：按钮级/字段级权限的具体规则未列全

【强制结构化检查】
8. 表单校验规则矩阵：对每个表单，逐字段检查是否定义了必填、格式校验、长度限制、取值范围、自定义校验、校验时机、校验失败提示文案——缺失任一项即为 BL 问题
9. 数据展示规则清单：对每个涉及格式化展示的字段，按类型检查展示规则——金额（小数位/千分位/货币符号/负数）、日期（格式/时区/相对时间阈值）、数字（千分位/精度/大数缩写）、手机号/身份证（脱敏规则）、文本（超长截断规则）、枚举/状态（文案/颜色/图标）、空值（null/undefined/空字符串的展示方式）
10. 业务规则清单化：提取 PRD 中涉及前端的每条业务规则，检查是否明确了触发条件、判断逻辑、通过/不通过时的前端表现
-->

---

## 七、需求歧义（AM 系列）

<!-- 为什么需要这部分：与后端审阅的歧义章节类似，但前端关注的是"不同理解导致不同的界面和交互方案"。比如 PRD 说"展示详情"，理解为"跳转新页面"和"侧滑抽屉"是两种完全不同的前端实现。提前对齐可避免页面级返工。 -->

> 需求歧义 = PRD 文字有多种理解方式，不同理解会导致前端选择不同的界面/交互/组件方案。

| # | 所属模块 | PRD 原文 / 位置 | 理解 A（及前端方案） | 理解 B（及前端方案） | 前端倾向 | 产品回复 |
|---|---------|---------------|-------------------|-------------------|---------|---------|
| AM-1 | {模块名} | {原文引用或章节} | {理解 A + 对应的前端方案} | {理解 B + 对应的前端方案} | {倾向哪个及理由} | |
| AM-2 | | | | | | |

<!-- 填写指引：
- 关键在于说明"不同理解对前端方案的影响差异"，让产品经理理解为什么需要澄清
- 前端倾向应附带用户体验和技术理由（如交互体验、实现复杂度、复用性）
- 如果两种理解的实现成本差异巨大，务必标明
- 常见的歧义类型包括：
  1. 交互方式型：模糊描述导致前端可选择完全不同的交互组件（如 Modal vs Drawer、Table vs Card）
  2. 术语不一致型：PRD 中的术语与原型中的标签文案不一致，导致前端展示偏差
  3. 展示方式型：数据的展示形式有多种理解（如列表 vs 树形、标签 vs 下拉）
-->

---

## 八、完整性缺失（CP 系列）

<!-- 为什么需要这部分：PRD 通常描述的是"Happy Path"（正常流程），但前端需要处理大量的非正常状态——空状态、加载状态、错误状态、边界条件。这些状态占据了前端代码的很大比例，如果 PRD 不定义，前端实现时只能自行猜测，后期必然产生争议。 -->

> 完整性缺失 = PRD 对功能的描述缺少前端实现所必需的关键信息。

| # | 所属模块 | 缺失内容 | PRD/原型位置 | 缺失原因说明 | 建议补充方式 | 产品回复 |
|---|---------|---------|-------------|------------|------------|---------|
| CP-1 | {模块名} | {缺失了什么} | {PRD 章节或原型页面} | {为什么前端需要这个信息} | {建议如何补充} | |
| CP-2 | | | | | | |

<!-- 常见的完整性缺失类型：
1. 空状态未定义：列表/数据区域无数据时展示什么？是否有引导操作？
2. 加载状态未定义：数据请求中用户看到什么？骨架屏还是 Loading？
3. 错误状态未定义：接口失败时展示什么？是否有重试按钮？
4. 边界条件未说明：文本超长如何处理（截断/换行/tooltip）？数值溢出如何展示？
5. 提示文案缺失：操作成功/失败的具体提示文案未给出
6. 无权限状态未定义：无权限访问时跳转哪里？还是展示 403 页面？
7. PRD-原型不一致：原型中存在的交互细节在 PRD 中没有对应描述
8. 多端适配未说明：PC/平板/手机下展示有何差异？哪些功能在移动端隐藏？
9. 浏览器兼容未定义：需要兼容哪些浏览器？最低版本是什么？
10. 操作确认文案缺失：删除/提交等操作的确认弹窗文案和按钮文案未定义

【强制结构化检查】
11. 页面全状态矩阵：对每个页面/核心组件，逐一检查 8 种状态是否在 PRD 中定义——初始状态（默认值/自动加载）、加载状态（骨架屏/Spinner/进度条）、正常状态（展示字段/格式/排列）、空状态（空状态图/文案/引导操作）、错误状态（错误提示/重试按钮）、部分状态（部分加载成功）、禁用状态（无权限/条件不满足）、满载状态（上限提示/操作限制）——缺失任一状态即为 CP 问题
12. 异常处理场景清单：对每个涉及接口调用的功能，逐一检查 7 种异常场景的前端处理是否定义——网络断开/超时、4xx 错误、5xx 错误、数据格式异常、并发冲突、Token 过期/登录态失效、批量操作部分成功——未定义即为 CP 问题
-->

---

## 九、非功能需求确认（NF 系列）

<!-- 为什么需要这部分：非功能需求直接决定前端技术选型和架构。比如是否需要 SSR、是否需要 PWA、是否需要无障碍支持，每个都会深刻影响前端架构。如果 PRD 不提，前端需要主动确认，避免上线后才发现架构无法满足要求。 -->

> 以下维度在 PRD 中未提及或未明确标准，请产品经理确认要求和优先级。

| # | 维度 | 当前 PRD 是否涉及 | 前端关注点 | 需确认的标准 | 产品回复 |
|---|------|-----------------|----------|------------|---------|
| NF-1 | 性能指标 | {是/否} | {如：首屏加载时间、交互响应时间、白屏时间} | {建议的标准} | |
| NF-2 | 响应式设计 | {是/否} | {如：需要适配的屏幕尺寸、断点规则} | | |
| NF-3 | 浏览器兼容 | {是/否} | {如：需要支持的浏览器及最低版本} | | |
| NF-4 | 无障碍 | {是/否} | {如：WCAG 等级、键盘导航、屏幕阅读器} | | |
| NF-5 | 国际化 | {是/否} | {如：多语言、日期/数字本地化、RTL} | | |
| NF-6 | SEO | {是/否} | {如：SSR/SSG 需求、meta 信息} | | |
| NF-7 | 离线/PWA | {是/否} | {如：离线访问、Service Worker、推送通知} | | |

---

## 十、技术实现建议

<!-- 为什么需要这部分：前端是"用户体验的直接实现者"，有些需求从前端技术角度看可以用更简单的方式实现同样的业务目标，甚至能提升用户体验。这是建议而非决策——最终决定权在产品经理。 -->

> 以下建议基于前端技术视角，旨在降低实现复杂度或提升用户体验，同时不损害业务目标。产品经理可酌情采纳。

| # | 相关功能 | 当前 PRD 方案 | 前端建议 | 建议原因 | 对用户体验的影响 | 产品回复 |
|---|---------|-------------|---------|---------|---------------|---------|
| SG-1 | {功能名} | {PRD 当前描述} | {前端建议的替代方案} | {技术原因} | {对用户体验的影响程度} | |
| SG-2 | | | | | | |

---

## 附录：审阅问题状态跟踪

<!-- 为什么需要这部分：提供一个集中的地方跟踪所有问题的处理进度。产品经理回复后，前端更新状态，确保没有遗漏。这张表在后续的评审会议中也可以作为议程使用。 -->

| 问题编号 | 状态 | 产品回复日期 | PRD 是否已修改 | 前端确认 |
|---------|------|------------|-------------|---------|
| UI-1 | {待回复/已回复/已关闭} | | {是/否/不需要} | {确认/仍有疑问} |
| IX-1 | | | | |
| CM-1 | | | | |
| PF-1 | | | | |
| BL-1 | | | | |
| AM-1 | | | | |
| CP-1 | | | | |
| NF-1 | | | | |
| SG-1 | | | | |
