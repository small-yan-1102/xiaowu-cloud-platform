---
trigger: always_on
---
# 测试用例模板: {项目名称}

> 使用方式：复制本模板，替换 `{项目名称}`，按阶段填写用例。
> 不涉及的阶段整节删除（如无数据迁移则删除阶段一）。
> 依赖文档：`{设计文档}`、`{补充文档}`。
> 用例编写规范见：`rules/tech-optimization-ai-testing-rules.md` 第六节。

---

## 测试阶段总览

> 根据项目实际情况裁剪，删除不涉及的阶段。

```
阶段一: 数据迁移/存量处理（如涉及）
  ├── 1.1 迁移前置校验
  ├── 1.2 迁移正向执行
  ├── 1.3 迁移幂等性验证
  └── 1.4 迁移后数据一致性校验

阶段二: 核心流程变更验证
  ├── 2.1 变更点A
  ├── 2.2 变更点B
  └── 2.N 端到端全流程

阶段三: 关联流程变更验证（如涉及）
  ├── 3.1 关联变更点A
  └── 3.N 关联变更点N

阶段四: 回归测试
  ├── 4.1 不变功能A
  └── 4.N 不变功能N

[可选] 阶段五: 并发测试
  → 条件: 开发未提供覆盖率报告

[可选] 阶段六: 性能测试
  → 条件: 数据量级大或有性能敏感操作
```

---

## 阶段一：数据迁移/存量处理

> 如项目不涉及数据迁移，删除本阶段。

### 1.1 迁移前置校验

#### TC-MIG-PRE-01: 迁移前环境检查

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 测试环境已部署新代码，数据库变更已执行 |
| **操作步骤** | |

```
步骤 1: 确认数据库变更已生效
  操作: {确认新表/新字段存在的 SQL}
  检查点: 表结构与设计文档一致

步骤 2: 记录迁移前数据量基线
  操作: {统计各表关键数据量的 SQL}
  检查点: 记录各项数量，作为迁移后对比基准
```

---

### 1.2 迁移正向执行

#### TC-MIG-01: 迁移步骤一 - {步骤描述}

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | {前置步骤已完成；测试数据已准备} |
| **操作步骤** | |

```
步骤 1: 记录迁移前状态
  操作: {查询待处理数据的 SQL}
  检查点: 记录关键记录的 ID 和状态

步骤 2: 执行迁移
  操作: {触发方式，如 PowerJob 手动触发}
  参数: {任务参数}
  等待: 任务执行完成

步骤 3: 验证处理结果
  操作: {用步骤1记录的 ID 查询处理后的状态}
  检查点:
    ✓ {检查点1}
    ✓ {检查点2}

步骤 4: 验证关联数据未被误改
  操作: {查询不应被影响的关联数据}
  检查点: 状态不变
```

| 预期结果 | {总结预期} |

> 如有多步迁移，按此格式依次添加 TC-MIG-02、TC-MIG-03 ...

---

### 1.3 迁移幂等性验证

#### TC-MIG-IDEM-01: 迁移重复执行

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 迁移已完整执行一次 |
| **操作步骤** | |

```
步骤 1: 记录当前数据状态快照
步骤 2: 再次执行完整迁移
步骤 3: 对比执行前后数据
  检查点:
    ✓ 无重复处理
    ✓ 各表记录数不变
    ✓ 无异常日志
```

---

### 1.4 迁移后数据一致性校验

#### TC-MIG-POST-01: 迁移后全局校验

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 迁移完成 |
| **操作步骤** | |

```sql
-- 校验 1: {校验描述}
{SQL}
-- 预期: {预期结果}

-- 校验 2: {校验描述}
{SQL}
-- 预期: {预期结果}
```

---

## 阶段二：核心流程变更验证

> 根据设计文档的变更概述，为每个变更点编写用例。

### 2.1 {变更点A 名称}

#### TC-{缩写}-01: {正向场景描述}

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | {数据状态、环境配置} |
| **操作步骤** | |

```
步骤 1: {操作描述}
  操作: {具体操作方式}
  检查点:
    ✓ {检查点}

步骤 2: {验证描述}
  操作: {验证 SQL 或日志搜索}
  检查点:
    ✓ {检查点}
```

| 预期结果 | {总结} |

---

#### TC-{缩写}-02: {反向/边界场景描述}

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | {不满足条件的数据} |
| **操作步骤** | |

```
步骤 1: {操作}
步骤 2: {验证}
  检查点:
    ✓ {应跳过/应拒绝/应降级}
```

---

#### TC-{缩写}-03 ~ NN: 批量跳过条件验证

> 如果某个操作有多个跳过/拦截条件，可以用表格批量列出。

| 用例 ID | 不满足的条件 | 操作 | 预期 |
|---------|------------|------|------|
| TC-{缩写}-03 | {条件1不满足} | {触发操作} | 跳过，无异常 |
| TC-{缩写}-04 | {条件2不满足} | 同上 | 同上 |
| TC-{缩写}-05 | {条件3不满足} | 同上 | 同上 |

---

### 2.N 端到端全流程

#### TC-E2E-01: {完整生命周期描述}

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 干净的测试环境，无存量干扰数据 |
| **操作步骤** | |

```
步骤 1: {触发起点}
  → 预期: {中间状态1}

步骤 2: {流转动作}
  → 预期: {中间状态2}

步骤 3: {流转动作}
  → 预期: {中间状态3}

步骤 N: 全链路数据验证
  操作: 按 {关联ID} 追踪完整数据链
  检查点: 每个环节状态正确，关联关系完整
```

---

## 阶段三：关联流程变更验证

> 如项目只有一个核心流程变更，无关联流程，删除本阶段。
> 结构与阶段二一致，不再重复模板。

---

## 阶段四：回归测试

> 基于变更确认表中"是否预期变更=否"的项，逐一验证。

#### TC-REG-01: {不变功能A}

| 项目 | 内容 |
|------|------|
| **优先级** | P0/P1 |
| **前置条件** | {前置条件} |

```
步骤 1: {执行原有操作}
步骤 2: 验证行为不变
  检查点:
    ✓ {与改造前行为一致}
```

---

#### TC-REG-02: {不变功能B}

> 同上格式，逐项添加。

---

## 阶段五：并发测试 [可选]

> 本阶段为可选项，不阻塞上线。仅在开发未提供单元/集成测试覆盖率报告、
> 或上线后出现并发相关问题时执行。

#### TC-CON-01: {并发场景描述}

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | {多实例/多线程环境} |

```
步骤 1: {模拟并发操作}
步骤 2: 验证
  检查点:
    ✓ 无重复数据
    ✓ 无数据不一致
```

---

## 阶段六：性能测试 [可选]

> 本阶段为可选项，不阻塞上线。仅在数据量级大或有性能敏感操作时执行。

#### TC-PERF-01: {性能场景描述}

| 项目 | 内容 |
|------|------|
| **优先级** | P2 |
| **前置条件** | {大数据量测试环境} |

```
步骤 1: {执行操作}
步骤 2: 记录耗时
  检查点: {耗时 < 阈值}
```

---

## 用例执行依赖关系

> 根据实际用例间的依赖填写。

```
{迁移用例依赖链（如有）}
  ↓
{功能验证用例（可并行）}
  ↓
{回归用例（可并行）}

[可选] 并发测试
[可选] 性能测试
```

## 用例优先级统计

| 优先级 | 数量 | 说明 |
|--------|------|------|
| P0 | {N} 个 | 必须全部通过才能上线 |
| P1 | {N} 个 | 应通过，可评估风险后决策 |
| P2 | {N} 个 | 建议执行，不阻塞上线 |
