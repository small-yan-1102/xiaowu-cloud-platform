---
description: 将 AI 测试执行结果同步到 Excel/Markdown 用例文档，生成覆盖率和通过率统计报告
---
请读取并严格执行 `.claude/skills/test-result-sync/SKILL.md` 中定义的完整工作流。

> 将AI直接执行测试的结果同步到Excel或Markdown测试用例文档，生成覆盖率和通过率统计报告。当需要同步测试结果、回填Excel、更新Markdown用例状态、生成测试执行报告时使用此技能。

技能所需的支撑文件（模板、检查清单等）位于 `.claude/skills/test-result-sync/` 目录下，以该目录为基准解析相对路径。
