---
description: 基于API文档对比现有mock数据生成接口联调实施计划。当用户需要进行接口联调、对接真实接口替换mock数据时使用。
---
使用 Read 工具读取 `.claude/skills/code-implementation/SKILL.md`，然后严格执行其中定义的完整工作流，不可跳过读取步骤。
支撑文件（模板、检查清单等）位于同目录下，以 `.claude/skills/code-implementation/` 为基准解析相对路径。
