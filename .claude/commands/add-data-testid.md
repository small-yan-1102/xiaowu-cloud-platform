---
description: 根据测试用例文档，在对应组件的 DOM 元素上添加 data-testid 属性
---
请读取并严格执行 `.claude/skills/add-data-testid/SKILL.md` 中定义的完整工作流。

> 根据测试用例文档，在对应组件的 DOM 元素上添加 data-testid 属性。当用户说"添加 data-testid"、"按用例打标签"、"添加测试 ID"时使用。

技能所需的支撑文件（模板、检查清单等）位于 `.claude/skills/add-data-testid/` 目录下，以该目录为基准解析相对路径。
