---
description: 读取 AI 测试执行报告，从失败用例中提取缺陷信息，生成结构化缺陷报告。支持输出到 Markdown 缺陷清单（供手动提交）或 TAPD/Jira 导入格式（CSV）。位于测试执行之后、发布门禁之前。
---
请读取并严格执行 `linscode/skills/iteration/testing/bug-sync/SKILL.md` 中定义的完整工作流。

技能所需的支撑文件（模板、检查清单等）位于 `linscode/skills/iteration/testing/bug-sync/` 目录下，以该目录为基准解析相对路径。
