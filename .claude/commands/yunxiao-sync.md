---
description: 将 Markdown 测试用例同步到阿里云效 Testhub，支持增量同步、模拟运行和模块过滤
---
请读取并严格执行 `.claude/skills/yunxiao-sync/SKILL.md` 中定义的完整工作流。

> 将 Markdown 测试用例同步到阿里云效 Testhub，支持增量同步、模拟运行和模块过滤。

技能所需的支撑文件（模板、检查清单等）位于 `.claude/skills/yunxiao-sync/` 目录下，以该目录为基准解析相对路径。
