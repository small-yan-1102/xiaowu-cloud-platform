---
description: 验收用例设计。基于PRD和技术文档，从业务验收视角设计验收用例，覆盖核心业务链路的流程、规则、语义、场景完整性，输出可供 acceptance-walkthrough 直接执行的验收用例套件。
---
## 加载顺序

1. **Base**：读取 `linscode/skills/iteration/testing/acceptance/acceptance-case-design/SKILL.md` 作为主工作流
2. **Override**：读取 `.claude/skills/acceptance-case-design/OVERRIDES.md`，将其中的「覆盖」和「新增」条目应用到 Base 工作流对应位置
3. **执行**：按合并后的完整工作流执行

> 支撑文件优先使用 `.claude/skills/acceptance-case-design/` 下的本地版本，不存在时回退到 `linscode/skills/iteration/testing/acceptance/acceptance-case-design/` 下的 Base 版本。
