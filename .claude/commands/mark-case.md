---
description: 交互式勾选用例执行状态。输入用例编号（可多个），AI 弹出五态选项供点击，自动定位套件文件并回写「执行清单」段。人工无需手打 emoji / 日期。
---
请读取并严格执行 `.claude/skills/mark-case/SKILL.md` 中定义的完整工作流。

技能所需的支撑文件位于 `.claude/skills/mark-case/` 目录下，以该目录为基准解析相对路径。
