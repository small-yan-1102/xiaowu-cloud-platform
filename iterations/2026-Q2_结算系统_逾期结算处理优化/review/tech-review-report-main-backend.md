# 技术文档测试审阅报告（逾期结算处理 - 后端+接口）

---

**审阅信息**

| 项目 | 内容 |
|------|------|
| 技术文档名称 | V4.5-迭代变更总纲 / 02-逾期结算处理-详细设计 / 逾期结算处理-API接口设计 |
| 文档类型 | 综合技术方案（后端技术设计 + 接口定义 + 数据库设计） |
| 功能模块 | 逾期结算处理 |
| 模块标识 | `main` |
| 关联 PRD | PRD-内容结算系统迭代-V4.5（2026-04-10） |
| 审阅人 | QA |
| 审阅日期 | 2026-04-15 |
| 期望回复日期 | 2026-04-17 |
| 审阅轮次 | 第1轮 |

---

## 一、审阅结论摘要

| 维度 | 结果 |
|------|------|
| 审阅的维度数 | 6 个（C, D, F, A, R, K） |
| 阻塞性问题（B） | 2 个 |
| 补充项（S） | 5 个 |
| 疑问项（Q） | 3 个 |
| 风险场景-确认项（AI标准已覆盖） | 2 个 |
| 风险场景-补充场景项（AI标准未覆盖） | 3 个 |
| 建议项（A） | 2 个 |
| AI 不可测风险登记 | 2 个 |
| 整体评估 | **需补充后进入** |

### 整体评估说明

技术文档整体质量较高，三份文档覆盖了变更总览、详细设计、接口定义三个层次，PRD 11 个需求项全部有对应设计。但存在 2 个阻塞性问题：（1）batchSplit 接口入参结构变更（从 `List<Long>` 改为对象），属于**破坏性变更**，技术文档未说明前端适配方案和灰度策略；（2）R1 占比校验的精度比较方式（`compareTo` vs `==`）在详细设计中描述为 10 位精度，但实际 DB 字段为 8 位精度，存在不一致。这两个问题解决后可进入测试设计。

> **开发回复（2026-04-15）**：2 个阻塞性问题均已解决——C-1-01：batchSplit 入参保持 `List<Long>` 不变，无破坏性变更，PRD AC6 为遗留文案已联系产品删除；C-2-01：R1 精度描述已修正，实现时增加容差保护。全部 17 项问题已逐一回复，整体评估建议更新为**可进入测试设计**。

---

## 二、需求一致性与风险场景

### 2.1 需求-技术一致性问题

| 编号 | 级别 | 技术文档位置 | PRD对照位置 | 问题描述 | 改进建议 | 开发回复 | 状态 |
|------|------|-----------|-----------|---------|---------|---------|------|
| C-1-01 | B | 详细设计 §2.2.2 | PRD §SET-02-01 AC5 | PRD 规定拆分成功提示"操作成功"，但详细设计 §2.2.2 返回 `DomiResponse.getSuccess()` 说明"不返回处理数量，符合 V4.5 PRD"。然而 PRD AC5 原文为"操作成功"，而 PRD AC6 的部分失败场景提示"X条成功，Y条失败"。技术文档的 batchSplit 是全事务回滚（任何步骤失败则全部回滚），**不支持部分成功**，与 PRD AC6 部分失败提示矛盾。 | 明确：batchSplit 是全部成功或全部失败（事务回滚），不存在"部分成功"场景。建议与产品确认删除 PRD AC6 或改为"操作失败，请重试"。 | **确认：不存在部分成功场景。** 产品经理在 PRD 审阅阶段已明确回复（BL-3）：采用全部回滚策略，一个失败全部回滚。PRD AC6 的"X条成功，Y条失败"是上一版遗留文案，与当前全回滚策略矛盾。**处理方案**：已联系产品删除 PRD AC6 或修改为"操作失败：{具体失败原因}，请检查后重试"。技术文档和现有代码（`@Transactional` 在 `batchSplit()` 方法上）均为整体事务，行为一致。此项**降级为非阻塞**。 | Resolved |
| C-2-01 | B | 详细设计 §3.1.3 | PRD §SET-03-01 R1 | R1 占比精度不一致：详细设计 §3.1.3 声称"占比为 10 位小数（`DECIMAL(14,10)`）"，但实际 DB 字段 `v_revenue_ratio` 为 `BigDecimal(8位)`（代码逆向文档确认精度8位）。10 位 vs 8 位的精度差异会导致 `compareTo()` 结果不同，R1 校验可能误判。 | 确认 DB 实际精度（8位还是10位），详细设计和代码保持一致。如 DB 为 8 位，则删除"10位小数"描述。 | **确认：两张表精度不同，QA 发现有效。** 代码走查结果：①`yt_month_channel_revenue_source.v_revenue_ratio` = 8位精度（`YTMonthChannelRevenueTaskServiceImpl:307` 使用 `divide(denominator, 8, HALF_UP)`）；②`yt_reversal_report.source_channel_revenue_ratio` = 10位精度（`YtMonthChannelRevenueMapper.xml:57` 使用 `ROUND(..., 10)`）。R1 比对的是两张不同表的占比总和，精度本就不同。**修复方案**：详细设计已删除"10位精度"的错误描述。R1 校验改为：两侧占比总和均**不取整**，直接用 `BigDecimal.compareTo()` 比较（忽略尾零差异）。由于两侧数据来源不同（分别从各自表查询），相同视频在两张表的占比理论上应精确一致（同源计算）。如因精度截断导致尾数差异，将在实现阶段补充容差逻辑（如 `|差值| < 1e-10` 视为相等）。此项**降级为非阻塞**，实现时加容差保护。 | Resolved |
| C-2-02 | S | 详细设计 §3.1.5 | PRD §SET-03-01 R3 | R3 目标金额查询条件 `received_status = 0` 写为"RECEIVED"，但 received_status=0 在现有代码中是"未到账"，received_status=1 才是"已到账"。详细设计中注释 `-- RECEIVED` 与实际语义矛盾。 | 确认 R3 查询应为 `received_status = 1`（已到账），或澄清 received_status 枚举含义。 | **确认：QA 发现有效，存在笔误。** 代码确认 `ConstantPool.ReceivedStatus.RECEIVED = 1`（已到账）、`UNRECEIVED = 0`（未到账）。R3 目标金额应从**已到账**的冲销报表中查询，正确条件为 `received_status = 1`。详细设计中如出现 `received_status = 0 -- RECEIVED` 属于笔误，已修正为 `received_status = 1 -- RECEIVED（已到账）`。注：详细设计 §3.1 的 R3 描述中未显式写 SQL 查询条件，R3 的"无归属金额"来源于 R1 步骤已查询的 `yt_reversal_report` 数据（`sign_channel_id = -1`），不涉及单独的 `received_status` 过滤。但在实现中会确保冲销报表查询条件包含 `received_status = 1`。 | Resolved |
| C-3-01 | S | 详细设计 §2.1.1 | PRD §SET-01-01 | 详细设计中 `synVideoComposition` 入参字段为 `publishedAt`（Date），但现有代码使用 `publishedDate`（String, yyyy-MM-dd）。字段名不一致可能导致反序列化失败。 | 确认入参字段名是 `publishedAt`（新增）还是 `publishedDate`（复用），统一命名。 | **澄清：不存在反序列化风险，但需说明两个字段的不同用途。** `VideoComposition` 实体中**两个字段都存在**：①`publishedAt`（`java.util.Date` 类型，精确到时间，用于逾期判定的时间计算）；②`publishedDate`（`String` 类型，`yyyy-MM-dd` 格式，用于列表展示和同步到 `video_composition_overdue.published_date`）。详细设计 §2.1.1 步骤4.3 使用 `publishedAt`（Date）做逾期红线计算是正确的（需要精确日期进行月份推算）；步骤6.1 `syncOverdueFields()` 同步 `publishedDate`（String）到 overdue 表用于展示也是正确的。两者不冲突，已在设计文档中补充注释说明两个字段的不同职责。 | Resolved |
| C-4-01 | S | API接口设计 §2.1.2 | PRD §SET-02-01 AC4 | PRD AC4 规定确认弹窗文案为详版（"将对勾选视频所在...是否确认执行？"），技术文档 API 接口层未提及弹窗文案（属前端实现）。但 API 设计中 batchSplit 的成功提示为"操作成功"，未提及处理条数。如前端需要展示"共处理X条"，接口需返回 count。 | 确认前端是否需要展示处理条数。如需要，batchSplit 响应需返回 `processedCount` 字段。 | **确认：保持现有设计（void 返回），不新增 processedCount。** 理由：①PRD AC5 成功提示仅为"操作成功"，无需展示条数；②现有代码 `batchSplit()` 为 void 方法，V4.5 不改变返回结构；③弹窗文案属前端实现，后端不涉及。如后续产品新增"展示处理条数"需求，可在下一迭代补充返回字段。 | Resolved |
| C-5-01 | Q | 详细设计 §2.3.1 | PRD §SET-04-01 | PRD 规定漏抓标签在**列表查询时动态判定**或**入库时打标**（TBD-7），技术文档选择了**入库时打标**方案（`video_tag` 字段写入 DB）。但对于已入库的历史数据（video_tag 默认=0），不会回溯重新打标，这意味着**历史记录的漏抓标签永远为0**。 | 确认：(1) 历史已入库的 VCO 记录是否需要补打 video_tag？(2) 如需补打，是否需要数据修复脚本？ | **确认：设计中已包含历史数据补打脚本，QA 可能遗漏了 §7.3 章节。** 详细设计 §7.3「历史数据修复脚本」已设计了两条补打 SQL：①对 `video_composition` 表：根据 `scraped_at > 发布次月15日 23:59:59` 将符合条件的记录 `video_tag` 设为 1；②对 `video_composition_overdue` 表：通过 `INNER JOIN video_composition` 同步补打。脚本在上线前统一执行。另外澄清：`video_tag` 默认值为 `NULL`（非0），NULL 表示"未判定/非漏抓"，1 表示"技术漏抓"。 | Resolved |
| C-6-01 | Q | 详细设计 §2.2.2 | PRD §SET-02-01 | 详细设计新增了 `BatchSplitRequest` DTO（包含 `ids` + `status`），这是**接口入参结构变更**（从 `List<Long>` 改为对象）。PRD 未提及此变更，属于技术侧自行设计。此变更合理（需要区分操作Tab），但需确认前端是否已知晓并适配。 | 属于合理的超范围设计，无需删除。但需确认前端已知晓此接口入参变更。 | **澄清：详细设计 §2.2.2 的入参仍然是 `List<Long> ids`，未新增 `BatchSplitRequest` DTO。** QA 可能参照了其他版本文档。实际设计方案：后端通过查询勾选记录自动提取 status 值（步骤3：查询记录后校验所有记录是否为同一状态），**不需要前端额外传 status 参数**。接口入参保持 `List<Long>` 不变（见 API 接口设计 §2.1.2），无破坏性变更，前端无需适配。 | Resolved |
| C-6-02 | Q | 详细设计 §3.1.7 | PRD §SET-03-01 | 详细设计新增了 `yt_month_cms_end 无记录` 时的错误处理："标记该组全部失败（原因：CMS 收益数据未导入），跳过"。PRD 未定义此场景（PRD 假设 CMS 数据总是存在的）。而现有代码中 cmsEnd 无记录时是**置零**而非失败。 | 确认：cmsEnd 无记录时应该是"失败阻断"（详细设计）还是"置零继续"（现有代码）？这是本期新行为还是与现有行为不一致的改动？ | **确认：保持现有代码行为——置零继续，不阻断。** QA 发现有效。代码确认（`OverdueSettlementImportHandler:335-339`）：`cmsEnd == null` 时将 revenue/usRevenue/sgRevenue 全部设为 `BigDecimal.ZERO`，不抛异常。详细设计中"标记失败"的描述属于过度设计，与现有行为不一致。V4.5 仅新增 R1~R6 校验逻辑，不改变 cmsEnd 缺失时的兜底行为。**修复方案**：已将详细设计 §3.1 中关于 cmsEnd 缺失的描述修正为"如果 cmsEnd 无记录，三种收益置为 0.00，继续处理（保持现有行为）"。 | Resolved |

### 2.2 AI标准已覆盖确认项

| 编号 | 风险类型 | 确认说明 | 对应AI标准条款 |
|------|---------|---------|-------------|
| C-9 | 操作不可逆风险 | 批量拆分前有确认弹窗（详细设计 §2.2.2 + PRD §3.2 安全要求），且整体事务回滚保证原子性 | 标准已覆盖不可逆操作确认机制 |
| C-12 | 权限边界 | 菜单级权限控制（财务/运营角色隔离）+ Sa-Token 认证（API 接口设计 §1.3） | 标准已覆盖权限边界用例 |

### 2.3 风险场景→补充AI测试场景

| 编号 | 风险描述 | AI可测性 | 建议的AI测试场景 | 开发回复 | 状态 |
|------|---------|---------|----------------|---------|------|
| C-7-01 | **导入并发竞态**：两个运营同时导入同月份数据，任务互斥检查（30分钟超时窗口）可能存在竞态——两个请求几乎同时通过互斥检查。 | 可API模拟 | 两个并发 `POST /import/async` 请求（相同月份），验证只有一个成功创建任务，另一个返回"当前存在导入中的任务" | **确认：风险有效。** 现有互斥检查基于 DB 查询"是否存在进行中的导入任务"，存在 TOCTOU 竞态窗口。当前产品使用场景为单人操作（运营专人负责），实际并发概率极低。V4.5 不做并发控制改造，记录为已知风险。如后续需要加固，可引入 Redis 分布式锁（`import:lock:{receiptedMonth}`）。建议 QA 纳入测试用例以记录行为基线。 | Acknowledged |
| C-7-02 | **拆分并发竞态**：两个财务同时对同频道记录执行批量拆分，`expandToSameDimensionRecords` 可能两次查到相同记录。 | 可API模拟 | 两个并发 `POST /batchSplit` 请求（相同频道），验证不会产生重复冲销表子集行 | **确认：风险有效，但现有代码已有防护。** `batchSplit()` 方法标注 `@Transactional`，且在步骤6中校验"pipelineId 已存在则抛异常"（冲销表子集的 pipelineId 唯一性校验）。两个并发拆分：第一个事务成功提交后，第二个事务会在 pipelineId 唯一性校验处抛异常并回滚。极端情况（两个事务同时到达校验点）依赖 DB 唯一索引兜底。建议 QA 纳入并发测试用例验证防护有效性。 | Acknowledged |
| C-10-01 | **R3 抹平后数据依赖**：导入成功后，若 `yt_reversal_report.unattributed_revenue` 被其他流程修改（如月度重算），已导入的抹平金额不会重新调整，可能导致账目不平。 | 不可直接测试 | 记录为已知风险：导入后的 VCO 金额不随冲销表变化重新计算。建议产品确认是否需要重新导入机制。 | **确认：已知风险，属于业务设计决策。** 导入是一次性操作，写入的金额为导入时刻的快照。如果冲销报表事后修改（月度重算），运营需要删除已导入数据后重新导入。现有代码已有"按 `receipted_month+cms+video_id` 判重"机制，重复导入会跳过已存在记录。如需覆盖导入，需先手动清理。此行为与产品设计一致（PRD 未要求自动重算），记录为已知限制。 | Acknowledged |

### 2.4 AI 不可测风险登记

| 编号 | 风险类型 | 风险描述 | 建议的专项测试方式 | 开发回复 | 状态 |
|------|---------|---------|----------------|---------|------|
| NR-1 | 性能 | 导入 20000 行 Excel + R1~R6 分组校验，处理时间是否在 30s 内完成（PRD §3.1 性能要求） | JMeter 压测：20000 行文件，测量端到端处理时间 | **确认：导入为异步任务，30s 不是接口响应时间而是后台处理时间。** 接口 `POST /import/async` 立即返回（创建异步任务），实际处理在后台线程执行。R1~R6 校验按频道分组处理，单频道内为串行计算（BigDecimal 运算），频道间无并行需求（PF-1 产品已确认无需考虑频道数量）。建议在 staging 环境用 20000 行实际数据测试后台处理时长，如超 30s 再优化（如批量查询优化、减少 DB 往返）。 | Acknowledged |
| NR-2 | 数据库 | 历史数据修复脚本（§7.3）执行时间和锁表影响，JOIN 两表 UPDATE 在百万级数据量下的性能 | 低峰期执行前先在 staging 环境测试，记录执行时间和锁等待 | **确认：将在 staging 环境预执行并记录性能指标。** 迭代变更总纲 §8.1 已说明：预计影响数千至数万条记录（非百万级），脚本执行前备份受影响数据，执行后验证记录数。三条修复 SQL 将分批执行：①死锁数据修复（status 0→3）；②已拆分 originalStatus 补写；③videoTag 补打（JOIN 查询）。低峰期执行，如单条 SQL 执行超 10s 将拆分为 LIMIT 分批更新。 | Acknowledged |

---

## 三、AI 可测试性问题清单

### 3.1 表单字段约束（F）

| 编号 | 级别 | 技术文档位置 | 问题描述 | 对AI测试的影响 | 改进建议 | 开发回复 | 状态 |
|------|------|-----------|---------|-------------|---------|---------|------|
| F-1 | S | API §2.1.2 | `BatchSplitRequest.status` 字段校验注解为 `@NotNull`，但业务要求值必须为 1 或 3。技术文档在业务逻辑层做了值校验（"status 不在 {1,3} 中 → 抛异常"），但缺少 `@Range` 或自定义注解在 DTO 层校验。 | AI 接口测试需覆盖 status=0/2/4 等非法值 | 建议在 DTO 层增加值域校验注解，或在文档中明确"DTO 层只校验非空，值域校验在 Service 层" | **澄清：如 C-6-01 回复，batchSplit 入参仍为 `List<Long>`，不存在 `BatchSplitRequest.status` 字段。** 后端通过查询记录自动提取 status，不依赖前端传值。此项不适用。如指列表查询接口的 `status` 参数：传入非法值（如4）返回空列表是合理行为（WHERE status=4 匹配不到记录），项目统一在 Service 层做业务校验，DTO 层仅做非空/格式校验。 | Resolved |
| F-2 | A | API §2.1.1 | `VideoCompositionOverdueQuery` 的 `status` 字段无值域校验，传入 status=4（不存在的状态）时返回空列表而非错误。 | AI 测试时传入非法 status 值，期望行为需明确（返回空 or 报错） | 可选改进：对不存在的 status 值返回明确错误提示 | **确认：返回空列表是预期行为，不做改动。** 这是项目通用模式——列表查询传入不匹配的筛选条件返回空结果集，不报错。前端 Tab 切换传固定 status 值（0/1/2/3），不存在用户手动输入非法值的场景。AI 测试预期：status=4 → 返回 `{"records": [], "total": 0}`。 | Resolved |

### 3.2 测试数据准备支撑（D）

| 编号 | 级别 | 技术文档位置 | 问题描述 | 对AI测试的影响 | 改进建议 | 开发回复 | 状态 |
|------|------|-----------|---------|-------------|---------|---------|------|
| D-1 | S | 详细设计 §2.1.1 | 登记状态分发（SET-01）的触发方式为"剧老板前端调用 synVideoComposition"，但**无独立的 HTTP 测试接口**可直接触发 status 分发。AI 测试需要跨系统操作或 MQ 模拟。 | 测试 status 0→1 和 0→3 路径需要通过剧老板系统触发或 DB 直接修改，无法通过结算系统 API 独立测试 | 建议提供测试环境可用的 MQ 消息发送工具或测试用 HTTP 触发接口 | **确认：SET-01 通过 MQ 消费触发（Tag=`distribution-video-composition`），非 HTTP 接口。** 测试方案：①测试环境可通过 RocketMQ 控制台手动发送消息模拟登记事件；②也可在剧老板测试环境直接操作登记触发 MQ 消息。V4.5 不新增测试用 HTTP 接口（避免引入非业务代码）。建议 QA 在联调阶段与剧老板前端协调测试环境触发路径。 | Acknowledged |
| D-2 | S | 详细设计 §3.1 | 导入 R1~R6 测试需要精确构造 `yt_month_channel_revenue_source`、`yt_month_cms_end`、`yt_reversal_report` 三张关联表的数据，但技术文档未提供测试数据构造 SQL 或 API。 | AI 测试导入校验规则需要 DB 级数据准备，无 API 可用 | 建议在测试环境提供数据初始化脚本或 seed data | **确认：将在开发完成后提供 R1~R6 各场景的测试数据构造 SQL。** 涉及三张表的关联数据：①`yt_reversal_report`（无归属记录，`sign_channel_id=-1`）；②`yt_month_channel_revenue_source`（视频级占比）；③`yt_month_cms_end`（频道CMS收益+调差）。开发阶段会编写覆盖 R1通过/阻断、R2零值清理、R3抹平、R4超阈值、R5全零、R6多收益类型的 seed data，测试阶段移交给 QA。 | Acknowledged |
| D-3 | A | API §2.1.2 | batchSplit 拆分后数据不可逆（status 变为 2），但无"取消拆分"或"恢复数据"的 API 接口。反复测试需要 DB 恢复。 | 每轮拆分测试后需手动 DB 恢复，增加测试维护成本 | 可选改进：提供测试环境数据恢复脚本 | **确认：将提供测试环境数据恢复脚本。** 脚本功能：①将指定记录的 `status` 从 2 回退到拆分前状态（通过 `original_status` 字段反推）；②清除 `operator_id/operator_name/operate_time/original_status`；③删除对应的冲销表子集行。仅在测试环境使用，不上线。 | Acknowledged |

### 3.3 异步流程等待策略（A）

| 编号 | 级别 | 技术文档位置 | 问题描述 | 对AI测试的影响 | 改进建议 | 开发回复 | 状态 |
|------|------|-----------|---------|-------------|---------|---------|------|
| A-1 | S | 详细设计 §4.1 | 登记事件处理（synVideoComposition）的触发方式在技术文档中描述为"剧老板前端调用 finance-service 内部接口（具体方式待剧老板确认：MQ / HTTP）"。**MQ 还是 HTTP 未确定**，影响 AI 测试的等待策略。 | 如果是 MQ，AI 需要等待消费延迟（30s+）；如果是 HTTP 同步调用，响应即完成。等待策略完全不同。 | 必须确认触发方式（MQ / HTTP），不同方式对应不同的测试等待策略 | **确认：触发方式为 MQ。** 代码已确认（`MQMessageListener` 监听 `Tag=distribution-video-composition`），现有实现为 RocketMQ 异步消费。AM-1 产品确认：状态分发在 `synVideoComposition` 方法中同步执行（即 MQ 消费内同步完成，非另起线程）。**测试等待策略**：MQ 消费通常 1~3 秒内完成，建议 AI 测试等待 5 秒后查询 DB 验证状态变更。如超时未变更，检查 MQ 消费日志。详细设计 §4.1 已明确为"MQ 消费（Tag=distribution-video-composition）"。 | Resolved |

### 3.4 排序/筛选规则精确性（R）

| 编号 | 级别 | 技术文档位置 | 问题描述 | 对AI测试的影响 | 改进建议 | 开发回复 | 状态 |
|------|------|-----------|---------|-------------|---------|---------|------|
| R-1 | S | 详细设计 §2.2.1 | status=3 Tab 排序规则为 `registration_time DESC`，但未说明 `registration_time` 相同时的次级排序字段（如 `id DESC`）。 | AI 验证排序结果时，同 registration_time 记录的顺序不确定 | 建议补充次级排序字段（如 `id DESC`），与 status=1 Tab 保持一致 | **确认：接受建议，补充次级排序字段。** 现有代码中所有 Tab 的排序均包含 `id DESC` 作为次级排序（`VideoCompositionOverdueMapper.xml`：status=0 为 `published_date DESC, id DESC`，status=1 为 `registration_time DESC, id DESC`，status=2 为 `operate_time DESC, id DESC`）。status=3 将与 status=1 完全一致：`ORDER BY registration_time DESC, id DESC`。已补充到设计文档。 | Resolved |

### 3.5 跨系统测试可执行性（K）

| 编号 | 级别 | 技术文档位置 | 问题描述 | 对AI测试的影响 | 改进建议 | 开发回复 | 状态 |
|------|------|-----------|---------|-------------|---------|---------|------|
| K-1 | S | 详细设计 §4.1 | 视频登记事件来自剧老板前端，但技术文档未说明剧老板测试环境的 URL 和触发接口路径。 | AI 无法自动化触发登记事件来验证 SET-01 功能 | 补充剧老板测试环境 URL 和视频登记接口路径 | **确认：剧老板属于外部系统，测试环境信息不在本项目技术文档范围内。** 建议 QA 联系剧老板前端团队获取测试环境地址和操作路径。替代方案：通过 RocketMQ 控制台直接发送 `distribution-video-composition` Tag 的消息体来模拟登记事件（消息格式与剧老板前端发送一致）。联调阶段会提供消息体样例 JSON。 | Acknowledged |

---

## 四、对AI测试设计的影响评估

### 4.1 可直接设计AI用例的功能模块

| 模块 | 说明 |
|------|------|
| SET-02 财务结算处理（列表查询 + 拆分） | 接口定义清晰，入参/响应结构完整，可直接设计 API 用例 |
| SET-02 导出适配 | 接口定义清晰，新增字段已标注 |
| SET-04 漏抓标签展示 | 打标逻辑清晰，接口返回 videoTag 字段，可通过 API 验证 |

### 4.2 需补充后才可设计AI用例的功能模块

| 模块 | 缺失信息 | 阻塞的AI用例设计范围 | 开发回复 | 状态 |
|------|---------|---------------------|---------|------|
| SET-01 登记状态分发 | 触发方式未确定（MQ/HTTP）；无独立测试触发接口 | 无法设计 0→1 和 0→3 状态分发的端到端用例 | **已确认为 MQ 触发（见 A-1 回复）。** 测试可通过 RocketMQ 控制台发送消息或剧老板测试环境操作触发。联调阶段提供消息体样例。 | Resolved |
| SET-03 导入 R1~R6 | R1 精度描述不一致（10位 vs 8位）；R3 received_status 语义矛盾；cmsEnd 缺失处理方式存疑 | R1 精确比较用例无法确定预期结果；R3 目标金额查询条件不确定 | **三个问题均已解决（见 C-2-01/C-2-02/C-6-02 回复）。** R1 精度已修正+增加容差保护；R3 查询条件确认为 `received_status=1`（已到账）；cmsEnd 缺失时置零继续（保持现有行为）。可正常设计 AI 测试用例。 | Resolved |

### 4.3 风险场景补充的AI测试用例建议

| 序号 | 来源风险编号 | 建议新增的AI用例场景 | 用例优先级 | 设计方法 |
|------|-----------|-------------------|-----------|---------|
| 1 | C-7-01 | 两个并发导入请求（同月份），验证任务互斥正确拦截第二个请求 | P1 | 并发测试 |
| 2 | C-7-02 | 两个并发拆分请求（同频道），验证不产生重复冲销表子集 | P1 | 并发测试 |
| 3 | C-10-01 | 导入后修改 yt_reversal_report.unattributed_revenue，验证 VCO 金额不变（已知风险记录） | P2 | 数据依赖测试 |
